# Configuration file for golangci-lint v2.1.6
version: "2"

run:
  go: '1.24'
  timeout: 5m

linters:
  default: standard
  enable:
    - bodyclose
    - errcheck
    - govet
    - ineffassign
    - noctx
    - staticcheck
    - unused
    - unconvert
    - gocyclo

issues:
  max-same-issues: 300
