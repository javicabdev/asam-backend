# Dependabot Triage — 2026-05-24

## Summary
- Total PRs reviewed: 11
- Recommended MERGE: 9
- Recommended REBASE-AND-MERGE: 0
- Recommended NEEDS-INVESTIGATION: 2
- Recommended CLOSE: 0
- SHA-pin regressions detected: 1

## Per-PR breakdown

### PR #92 — chore(deps): bump golang from 1.26.2 to 1.26.3
- **Recommendation:** NEEDS-INVESTIGATION
- **Mergeable:** unknown (stale CI from 2026-05-11; no recent merge-conflict check)
- **CI status:** green (all 4 jobs passed on 2026-05-11)
- **SHA-pin check:** REGRESSION — diff shows `FROM golang:1.26.3-alpine AS builder` and `FROM golang:1.26.3` with no `@sha256:<digest>` in either Dockerfile or Dockerfile.dev
- **Breaking changes:** none (patch bump)
- **Notes:** Must add `@sha256:<full-digest>` to both FROM lines before merging; without a digest pin this is a supply-chain regression relative to the project's pinning policy.

### PR #96 — chore(deps): bump github.com/golang-migrate/migrate/v4 from 4.18.3 to 4.19.1
- **Recommendation:** MERGE
- **Mergeable:** unknown (CI green; no explicit conflict signal)
- **CI status:** green (all 4 jobs passed on 2026-05-13)
- **SHA-pin check:** N/A
- **Breaking changes:** none — removes `hashicorp/go-multierror` and `go.uber.org/atomic` dependencies (internal refactor); public API unchanged
- **Notes:** Minor bump with dependency cleanup; CI clean.

### PR #97 — chore(deps): bump cloud.google.com/go/storage from 1.57.2 to 1.62.1
- **Recommendation:** MERGE
- **Mergeable:** unknown (CI green; no explicit conflict signal)
- **CI status:** green (all 4 jobs passed on 2026-05-13)
- **SHA-pin check:** N/A
- **Breaking changes:** none mentioned in commits; only features and chore changes
- **Notes:** Large go.sum churn (+60/-60) due to transitive GCP SDK updates; CI clean.

### PR #98 — chore(deps): bump go.uber.org/zap from 1.27.0 to 1.28.0
- **Recommendation:** MERGE
- **Mergeable:** unknown (CI green; no explicit conflict signal)
- **CI status:** green (all 4 jobs passed on 2026-05-13)
- **SHA-pin check:** N/A
- **Breaking changes:** none — v1.27.1 fixes race in `WithLazy` and nil panic in `Object`; v1.28.0 adds `CheckPreWriteHook` (additive)
- **Notes:** Also pulls in `go.yaml.in/yaml/v3` as new transitive dep; routine minor bump.

### PR #102 — chore(deps): bump the github-actions group across 1 directory with 7 updates
- **Recommendation:** NEEDS-INVESTIGATION
- **Mergeable:** yes (MERGEABLE / CLEAN)
- **CI status:** green (all 5 jobs passed on 2026-05-18)
- **SHA-pin check:** OK — all `uses:` lines in the diff use `@<40-char-sha> # v<version>` format (e.g. `actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd  # v6.0.2`); no tag-only pins observed
- **Breaking changes:** Several major version bumps that include breaking changes:
  - `actions/download-artifact` v8: hash mismatches now error by default (was a warning); migrated to ESM
  - `actions/github-script` v9: `require('@actions/github')` no longer works in scripts; `getOctokit` is now an injected function parameter — `const getOctokit = ...` will throw `SyntaxError`
  - `google-github-actions/setup-gcloud` v3: `skip_tool_cache` option removed
  - `softprops/action-gh-release` v3: Node 20 → Node 24 runtime
  - All actions: Node 20 → Node 24 runtime (requires runner >= v2.329.0)
- **Notes:** SHA pins are correct, but multiple major-version bumps carry breaking changes. Review the DAST workflow's `github-script` usage (it uses `require('fs')` and `fs` functions, not `require('@actions/github')`) — that specific pattern should be safe, but verify no other script blocks use the breaking `require('@actions/github')` pattern before merging.

### PR #103 — chore(deps): bump github.com/prometheus/client_golang from 1.22.0 to 1.23.2
- **Recommendation:** MERGE
- **Mergeable:** yes (MERGEABLE / CLEAN)
- **CI status:** green (all 4 jobs passed on 2026-05-18)
- **SHA-pin check:** N/A
- **Breaking changes:** v1.23.0 requires minimum Go 1.23 (repo uses Go 1.26 — compatible); no API changes in v1.23.1/v1.23.2
- **Notes:** Also bumps `prometheus/common` to v0.66.1 which drops `grafana/regexp` and `go.uber.org/atomic`; adds `go.yaml.in/yaml/v2` as replacement for `gopkg.in/yaml.v2`.

### PR #104 — chore(deps): bump golang.org/x/crypto from 0.46.0 to 0.51.0
- **Recommendation:** MERGE
- **Mergeable:** yes (MERGEABLE / CLEAN)
- **CI status:** green (all 4 jobs passed on 2026-05-18)
- **SHA-pin check:** N/A
- **Breaking changes:** none — security and stdlib-forwarding improvements only
- **Notes:** Also bumps several transitive `golang.org/x/*` packages (mod, net, sync, sys, text, tools); security-relevant dependency, prioritise this.

### PR #46 — chore(deps): bump gorm.io/driver/postgres from 1.5.11 to 1.6.0
- **Recommendation:** MERGE
- **Mergeable:** unknown (CI green; no explicit conflict signal)
- **CI status:** green (all 4 jobs passed on 2026-05-13)
- **SHA-pin check:** N/A
- **Breaking changes:** none — adds features (dynamic timestamp ScanLocation, varchar limit detection, CONCURRENT index option), fixes CVE-2024-45337 via crypto bump
- **Notes:** Pair with PR #60 (gorm.io/gorm bump) since postgres driver depends on gorm core; merge #60 first.

### PR #51 — chore(deps): bump github.com/vektah/gqlparser/v2 from 2.5.30 to 2.5.33
- **Recommendation:** MERGE
- **Mergeable:** unknown (CI green; no explicit conflict signal)
- **CI status:** green (all 4 jobs passed on 2026-05-13)
- **SHA-pin check:** N/A
- **Breaking changes:** none — bug fixes (repeatable directives, negative Position.Column) and additive validator features
- **Notes:** Core GraphQL parser for this project; patch-level fixes only.

### PR #60 — chore(deps): bump gorm.io/gorm from 1.26.0 to 1.31.0
- **Recommendation:** MERGE
- **Mergeable:** unknown (CI green; no explicit conflict signal)
- **CI status:** green (all 4 jobs passed on 2026-05-13)
- **SHA-pin check:** N/A
- **Breaking changes:** none — large version range (1.26.0 → 1.31.0) but all changes are additive (Generics API extensions, DefaultContextTimeout, slog integration, data-race fixes)
- **Notes:** Target version is `1.31.0` — confirmed NOT the blocked `1.31.1` (go-gorm/gorm#7686 Preload regression). Safe to merge. Merge this before PR #46 (postgres driver).

### PR #62 — chore(deps): bump golang.org/x/time from 0.12.0 to 0.15.0
- **Recommendation:** MERGE
- **Mergeable:** unknown (CI green; no explicit conflict signal; note: Dependabot auto-rebase disabled after 30 days open)
- **CI status:** green (all 4 jobs passed on 2026-05-13)
- **SHA-pin check:** N/A
- **Breaking changes:** none — only upgrades the `go` directive and uses `time.Time.Equal` instead of `==`
- **Notes:** PR has been open >30 days; auto-rebase disabled. May need a manual rebase to resolve potential drift, but diff itself is trivial.

## Recommended merge order
Ordered list, lowest-risk first. Justify briefly.

1. **PR #104** — `golang.org/x/crypto` 0.51.0: security-relevant crypto library, MERGEABLE/CLEAN, minimal diff, no breaking changes.
2. **PR #103** — `prometheus/client_golang` 1.23.2: MERGEABLE/CLEAN, no functional changes, only dependency cleanup in transitive graph.
3. **PR #51** — `gqlparser/v2` 2.5.33: core GraphQL parser, pure bug fixes, green CI.
4. **PR #62** — `golang.org/x/time` 0.15.0: trivial change; verify no merge conflict first given age of PR.
5. **PR #98** — `go.uber.org/zap` 1.28.0: additive minor bump, green CI.
6. **PR #60** — `gorm.io/gorm` 1.31.0: merge before #46; confirmed target is not the blocked 1.31.1.
7. **PR #46** — `gorm.io/driver/postgres` 1.6.0: merge after #60 since driver depends on gorm core.
8. **PR #96** — `golang-migrate/migrate/v4` 4.19.1: drops hashicorp deps, green CI.
9. **PR #97** — `cloud.google.com/go/storage` 1.62.1: large go.sum churn but CI clean; merge after simpler ones settle.
10. **PR #102** — github-actions group bump: MERGEABLE/CLEAN, all SHA-pinned correctly, but verify `github-script` usage in workflows does not hit the `require('@actions/github')` breaking change before merging.
11. **PR #92** — Docker `golang:1.26.3`: do NOT merge until `@sha256:<digest>` pins are added to both Dockerfiles.
